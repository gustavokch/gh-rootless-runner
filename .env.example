# Target GitHub Repository (Format: owner/repo)
# Used as fallback if -r flag is not provided to deploy_runner.sh
GITHUB_REPO=your-user/your-repo

# The following variables will be automatically populated by deploy_runner.sh:
# RUNNER_URL=...
# RUNNER_JIT_CONFIG=...
